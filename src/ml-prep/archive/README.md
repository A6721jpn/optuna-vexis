# archive

Legacy ML assets were moved here to isolate production runtime code from training utilities.

- `legacy_ai_v0/`: archived scripts/docs/datasets copied from `cad-automaton/src/ai-v0`.
  - Large generated training CSV files are intentionally omitted.

Artifacts are intentionally not used by v2 runtime.
